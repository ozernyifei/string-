#ifndef CHECK_SSCANF_H_SENTRY
#define CHECK_SSCANF_H_SENTRY

#include <check.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

void suite_add_sscanf(Suite *s);
void suite_add_s21_sscanf(Suite *s);

#endif